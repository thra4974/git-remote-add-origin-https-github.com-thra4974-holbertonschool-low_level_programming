#include "holberton.h"

/**
 * _islower - checks for lowercase character
 *
 * Return: Always 1
 * @c: parameter for _islower
 */

int _islower(int c)

{
	if (c >= 97 && c <= 122)


		return (1);


	else
		return (0);

}
