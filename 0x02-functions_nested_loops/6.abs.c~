#include "holberton.h"

/**
 * _abs - Computes absolute value of an integer
 *
 * Return: Always 0.
 */
int _abs(int)

{

	if (int < 0)
	{
		_putchar(int * -1);
	}

	else
	{
		_putchar(int);
	}

	return (0);

}
