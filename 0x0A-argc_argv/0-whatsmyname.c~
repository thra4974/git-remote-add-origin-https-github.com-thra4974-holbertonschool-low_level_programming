#include "holberton.h"
#include <stdio.h>

/**
 * main - Program prints name.
 * @argc: argument count.
 * @argv: pointer to argument vector.
 * Return: 0.
 */

int main (int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	(void) argc;

	return (0);
}
