#include "holberton.h"

/**
 * print_rev - prints string in reverse.
 * @s: string gets reversed.
 * Return: nothing.
 */

void print_rev(char *s)
{
	int i;

	while (i < s['\0'])
	{
		_putchar(s[i]);
		i--;
	}

	_putchar('\n');
}
