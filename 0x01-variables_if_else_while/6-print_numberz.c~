#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
	char num = num - 0;

	char newline = '\n';

	for (num = 'A'; num <= 'J'; num++)

	{
		putchar(num);

	}

	putchar(newline);

	return (0);

}
